<%= form_with(model: inscricao, local: true) do |form| %>
  <% if inscricao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inscricao.errors.count, "error") %> prohibited this inscricao from being saved:</h2>

      <ul>
      <% inscricao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class='container'>
  <div class="field form-group">
    <%= form.label :nome %><br>
    <%= form.text_field :nome, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :matricula %><br>
    <%= form.number_field :matricula, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :curso_id %>
    <%= collection_select(:inscricao, :curso_id, @cursos, :id, :titulo) %>
  </div>

  <div class="actions form-group text-center">
    <%= form.submit 'Cadastrar', class:'btn btn-success' %>
    <%= link_to 'Voltar', inscricaos_path, class:'btn btn-secondary' %>
  </div>
  
  </div>
<% end %>
